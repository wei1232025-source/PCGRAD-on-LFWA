# PCGRAD-on-LFWA
We implement the gradient surgery algorithm and validate its performance when handling multi-objective conflicts. To achieve this, this experiment constructs a conflicting task using LFWA database.
